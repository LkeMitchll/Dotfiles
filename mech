# FN + (R)CTRL -> [KEY] -> [ACTION] -> PN

m = 'mech' + enter
v = 'vim' + enter

backspace = CTRL + ALT + DEL

S = CTRL-A + S    Choose session TMUX
s = CTRL-A + s    TMUX sync mode

h = CTRL-A + h    TMUX switch pane left
j = CTRL-A + j    TMUX switch pane down
k = CTRL-A + k    TMUX switch pane up
l = CTRL-A + l    TMUX switch pane right
