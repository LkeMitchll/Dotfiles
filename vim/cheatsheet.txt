.                       Repeat last command
%                       goto matching bracket

n                       Next highlight
N                       Previous highlight

:tabs                   list all tabs
:tabm 0                 move current tab to first
:tabm                   move current tab to last
:tabn                   open next tab
:tabp                   open previous tab
:tabfirst               open first tab

:noh                    Reset /search highlight

:grep 'term' ../*.scss  Search for term in folder or files
:cn[ext]                Jump to next grep entry
:cp[revious]            Jump to previous grep entry

<C-A>                   Increment number
<C-X>                   Decrease number

42G                     Goto specific line number

qq	                    start recording to register q
q	                      stop recording
@q	                    execute your macro
@@	                    execute your macro again
15@q                    execute the macro 15 times

ma                      create a mark 'a'
`a                      goto mark 'a'

ctrl + u                Up page
ctrl + d                Down page

di"                     delete contents inside "" (swap out enclosing tag)

gUw                     Convert word to uppercase
guw                     Convert word to lowercase

ds"                     delete surrounding ""
cs"'                    substitute " for '
ysw"                    add surround to word object
ys2w"
ys$"
yss"                    add surround to entire line
V S<p>                  Line-wise mode

:lnext                  Next syntastic error.

<leader>c<space>        Toggle comment
<leader>cs              Toggle sexy comment
<leader>c$              Toggle comment from cursor to EOL
<leader>cy              Yank before comment

<leader>a:              align all statements on ':'

:s/foo/bar/g	          Change each 'foo' to 'bar' in the current line.
:%s/foo/bar/g	          Change each 'foo' to 'bar' in all lines.
:5,12s/foo/bar/g	      Change each 'foo' to 'bar' for all lines from line 5 to line 12 inclusive.
:'a,'bs/foo/bar/g	      Change each 'foo' to 'bar' for all lines from mark a to mark b inclusive (see Note below).
:'<,'>s/foo/bar/g	      When compiled with +visual, change each 'foo' to 'bar' for all lines within a visual selection. Vim automatically appends the visual selection range ('<,'>) for any ex command when you select an area and enter :. Also, see Note below.
:.,$s/foo/bar/g	        Change each 'foo' to 'bar' for all lines from the current line (.) to the last line ($) inclusive.
:.,+2s/foo/bar/g	      Change each 'foo' to 'bar' for the current line (.) and the two next lines (+2).
:g/^baz/s/foo/bar/g	    Change each 'foo' to 'bar' in each line starting with 'baz'.

:vim foo * | cw         Search for foo in every file in the current directory
:vim foo *.js | cw      Search for foo in every JavaScript file in the current directory
:vim foo ~/foo/**/*.js | cw   Search for foo in every JavaScript file in the current directory recursively
:vim <C-r><C-w> * | cw  Search for the current word in every file in the current directory
:ccl                    Close the 'quickfix' window

:args *.txt             "
:argdo %s/foo/bar/g     Perfom an action across all files
:argdo update           "

:reg                    View previous deletes (register)
"2p                     Put register 2 into buffer

:diffg RE               difftool get from REMOTE
:diffg BA               difftool get from BASE
:diffg LO               difftool get from LOCAL
[c                      jump to next conflict
]c                      jump to previous conflict

<C-W> x                 Switch current split with the one on the right

:mksession ~/mysession.vim    Create a session to return to later
:source ~mysession.vim  Open a previous session file
